const tarefas = ["Estudar", "Treinar", "Ler"];  

gerenciarTarefas(tarefas, "adicionarFim", "Dormir");  

console.log(tarefas);

// Resultado esperado: ["Estudar", "Treinar", "Ler", "Dormir"



function gerenciarTarefas(tarefas, ação,nova tarefa)[
    switch(ação)]
    case "adicionarInicio"
    if(NovaTarefa !==undefined)[
    tarefas.push(NovaTarefa;)
    ]
    break;
    case"RemoverInicio"
    tarefas.shift();
    break;
    case"removerFim";
    tarefas.pop();
    break;
    default:
        console.log("Ação Invalida.");






        const numeros = [10, 20, 30, 40, 50,];

        for (let i = 0; 1 )
